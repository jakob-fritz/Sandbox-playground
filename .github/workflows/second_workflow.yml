name: Workflow B

on:
  push:

jobs:
  Job_B_1:
    runs-on: ubuntu-latest
    steps:
      - name: Create one file
        run: |
          echo "Job is named: Job B 1" > workflow_c.txt
      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: workflow_c
          path: workflow_c.txt
        
  Job_B_2:
    runs-on: ubuntu-latest
    steps:
      - name: Create another file
        run: |
          echo "Job is named: Job B 2" > workflow_d.txt
      - name: Upload results
        uses: actions/upload-artifact@v4
        with:
          name: workflow_c
          path: workflow_c.txt
